class DirectionDetails{
  int distanceValue;
  int durationValue;
  String distanceText;
  String durationText;
  String encodedPoints;

  DirectionDetails({this.distanceValue, this.distanceText, this.durationText, this.durationValue, this.encodedPoints});
}